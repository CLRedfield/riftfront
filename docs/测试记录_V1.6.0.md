# 《裂界战线》V1.6.0 测试记录

测试日期：2026-08-17

## 1. 静态资源覆盖

- 卡牌数据总数：118
- 单位与Boss卡牌：76
- 建筑卡牌：17
- 单位缩略图：76 / 76
- 建筑缩略图：17 / 17
- 卡牌建筑动画：17 / 17
- 核心与前哨动画：4
- 缺失路径：0
- 四帧动画尺寸异常：0

## 2. JavaScript 语法检查

对 `js/*.js` 全部执行 `node --check`，无语法错误。

同时修复了 V1.5.0 美术补丁直接写入冻结 `RF.CARD_ART` 对象的问题，现改为复制、合并后重新冻结，避免严格模式下抛出 `TypeError`。

## 3. 运行时模拟检查

在完整脚本加载顺序下执行 Node VM 冒烟测试：

- `RF.VERSION`：1.6.0
- `RF.CARDS`：118
- `RF.THUMBNAIL_ART`：93
- `RF.BUILDING_ANIMATIONS`：21
- 玩家与敌方建筑均可生成
- `drawBuildingSprite` 静止帧与攻击帧可调用
- 玩家/敌方核心与前哨动画可调用
- UI 卡牌 HTML 正确引用 `assets/card_thumbnails/units/scouts.webp`

## 4. Chromium DOM 冒烟测试

使用 Chromium 执行完整 DOM 流程，并通过本地资源拦截加载游戏文件：

- 主菜单成功显示 V1.6.0；
- 套牌编辑器共检测到 57 个带图像卡牌区域，其中 55 个使用 V1.6 新缩略图；
- 直接进入第一关后成功创建 1280×720 战场；
- 玩家自动炮与敌方拼装炮塔成功生成并绘制；
- 共处理 116 次本地资源请求；
- 404：0；
- JavaScript 页面错误：0；
- 控制台错误：0。

## 5. 结论

V1.6.0 的缩略图映射、建筑动画映射、核心/前哨动画、脚本加载与本地离线运行链路均通过检查。
